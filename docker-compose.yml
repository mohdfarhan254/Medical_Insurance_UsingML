

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    networks:
      - fitinsure-net

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    networks:
      - fitinsure-net
    depends_on:
      - backend

networks:
  fitinsure-net:
